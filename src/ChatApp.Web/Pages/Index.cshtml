@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}

<div class="auth-card">
    <h1>Welcome, @User.Identity!.Name</h1>
    <p>You are logged in. The chatroom will be available here in the next iteration.</p>
</div>
